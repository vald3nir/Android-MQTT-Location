<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE"/>

    <application>
        <activity android:name="com.android.location.agent.view.AgentActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <receiver
            android:name="com.android.location.agent.service.BootDeviceReceivers"
            android:enabled="true"
            android:exported="true"
            android:installLocation="internalOnly">
            <intent-filter>
                <!-- Handles cold reboot (power off/on) -->
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <!-- Handles quick restart -->
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>
        <service
            android:name="com.android.location.agent.service.LocationService"
            android:enabled="true"
            android:exported="true" />
    </application>
</manifest>